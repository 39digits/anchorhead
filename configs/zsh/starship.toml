format = """
$username\
$hostname\
$directory\
$git_branch\
$git_state\
$git_status\
$php\
$nodejs\
$docker_context\
$cmd_duration\
$line_break\
$python\
$character"""

[directory]
style = "blue"
# Disable truncating the path to the git repository root
truncate_to_repo = false
# Set the truncation length to 0 to show the full path
truncation_length = 0

[character]
success_symbol = "[❯](purple)"
error_symbol = "[❯](red)"
vimcmd_symbol = "[❮](green)"

[git_branch]
format = "[$branch]($style)"
style = "bright-black"

[git_status]
format = "[[(*$conflicted$untracked$modified$staged$renamed$deleted)](218) ($ahead_behind$stashed)]($style)"
style = "cyan"
conflicted = ""
untracked = ""
modified = "​"
staged = "​"
renamed = "​"
deleted = "​"
stashed = "≡"

[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bright-black"

[cmd_duration]
format = "[$duration]($style) "
style = "yellow"

[python]
format = "[$virtualenv]($style) "
style = "bright-black"

[php]
format = '[${symbol} (${version} )]($style)'
symbol = "🐘"
style = "bright-black"

[nodejs]
format = '[${symbol} (${version} )]($style)'
symbol = "⬢"
style = "bright-black"

[docker_context]
format = '[🐋 $context](bright-black) '